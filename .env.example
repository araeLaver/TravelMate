# TravelMate 환경 변수 설정 예시
# 실제 사용 시 .env 파일로 복사하고 값을 수정하세요

# 데이터베이스 설정
DB_PASSWORD=your_secure_db_password_here
DB_HOST=localhost
DB_PORT=5432
DB_NAME=travelmate
DB_USERNAME=travelmate_user

# Redis 설정
REDIS_PASSWORD=your_secure_redis_password_here
REDIS_HOST=localhost
REDIS_PORT=6379

# JWT 설정 (최소 32자 이상의 안전한 문자열)
JWT_SECRET=your_very_secure_jwt_secret_key_at_least_32_characters_long

# CORS 설정
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:80,https://yourdomain.com

# 파일 업로드 설정
FILE_UPLOAD_PATH=/app/uploads/
MAX_FILE_SIZE=5MB

# 외부 서비스 API 키
FCM_SERVER_KEY=your_fcm_server_key_here
GOOGLE_MAPS_API_KEY=your_google_maps_api_key_here

# 이메일 서비스 (선택사항)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your_email@gmail.com
SMTP_PASSWORD=your_app_password
SMTP_FROM=noreply@travelmate.app

# 모니터링 (선택사항)
GRAFANA_PASSWORD=admin

# SSL 설정 (프로덕션)
SSL_CERT_PATH=/etc/ssl/certs/travelmate.crt
SSL_KEY_PATH=/etc/ssl/private/travelmate.key

# 로깅 레벨
LOG_LEVEL=INFO

# 캐시 TTL (초)
CACHE_TTL_USER=3600
CACHE_TTL_GROUP=1800
CACHE_TTL_RECOMMENDATION=600

# Rate Limiting 설정
RATE_LIMIT_ENABLED=true
RATE_LIMIT_REQUESTS_PER_MINUTE=60

# 보안 설정
SECURITY_HEADERS_ENABLED=true
CSRF_PROTECTION_ENABLED=true

# 개발 모드 설정
DEVELOPMENT_MODE=false
DEBUG_MODE=false